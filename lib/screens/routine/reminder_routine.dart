import 'package:flutter/material.dart';


class RemeinderScreen extends StatelessWidget {
const RemeinderScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('RemeinderScreen'),
     ),
   );
  }
}